name: My AI Unified — legacy production repair
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy workflow disabled. Use emergency-unified-deploy.yml for production deployment.'
