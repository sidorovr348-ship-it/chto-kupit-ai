name: My AI Unified — emergency public deployment
on:
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write

# Manual emergency workflow only. It must never race the canonical deployment.
jobs:
  disabled-by-default:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Manual emergency workflow. Canonical deployment owns the production tunnel.'
