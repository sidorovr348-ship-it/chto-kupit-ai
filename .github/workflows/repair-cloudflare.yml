name: Repair Cloudflare tunnel (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled: My AI Unified uses only the canonical Quick Tunnel repair workflow.'
