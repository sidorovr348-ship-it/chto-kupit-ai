name: Deploy My AI Unified to GitHub Pages (disabled duplicate)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled: canonical full-chain workflow owns GitHub Pages deployment.'
