name: Verify My AI Unified features (legacy)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy feature check disabled. Full-chain verification is part of emergency-unified-deploy.yml.'
