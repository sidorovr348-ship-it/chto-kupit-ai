name: Restore proven Quick Tunnel (disabled duplicate)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled: the canonical full-chain repair is .github/workflows/repair-my-ai-tunnel.yml'
