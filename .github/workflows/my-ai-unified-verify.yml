name: My AI Unified live verification (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled: public verification is performed only by the canonical full-chain repair workflow.'
