name: Unify Alice with central Dispatcher (legacy)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy workflow disabled. Alice is verified by emergency-unified-deploy.yml.'
